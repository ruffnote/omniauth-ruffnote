<% if @signed_in %>
  <p>
    <%= image_tag @image, size: '24x24' %>
    <%= @nickname %>
  </p>

  <ul>
    <% @teams.each do |team| %>
      <li><%= link_to team['name'], "#{team['html_url']}", target: '_blank' %></li>
    <% end %>
  </ul>
<% else %>
  <%= link_to 'Sign in', '/auth/ruffnote' %>
<% end %>

